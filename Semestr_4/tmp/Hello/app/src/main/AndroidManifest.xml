<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pl.edu.pwr.swim.chilczuk.hello">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>

<!--
Manifest: GŁÓWNY PLIK DEFINIUJĄCY APLIKACJĘ
• czego wymagamy od komputera
• znajdują się tu wszystkie aktywności, muszą tu być zdefiniowane

minSDK - aplikacja działa na tym API i wyższym
maxSDK - aplikacja działa na tym API i niższym
targetSDK - dla jakiego SDK aplikacja jest dedykowana; domyślnie jest to minSDK

to co jest w Gradle ma wyższość nad Manifestem
-->